# Python 3.12–3.14 (Streamlit Cloud uses 3.14). Prefer versions with pre-built wheels.
# Core (app + scripts)
pandas>=2.2.0,<3
numpy>=2.0,<3
pyyaml>=6.0
python-dotenv>=1.0.0
pytz>=2024.1
requests>=2.28
click>=8.1

# Streamlit and UI (streamlit>=1.40 allows Pillow 10+; Pillow 9.x fails to build on 3.14)
streamlit>=1.40.0
pillow>=10.0.0
streamlit-aggrid==0.3.4.post3

# File watcher (Streamlit / reload)
watchdog>=4.0

# Optional: CLI/automation (used by rates/pull, rates/push, or external scripts)
selenium>=4.18
webdriver-manager>=4.0
